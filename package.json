{
  "private": true,
  "name": "yuki-code",
  "version": "0.2.0",
  "devDependencies": {
    "@types/node": "^13.13.1",
    "opentype.js": "^1.3.3",
    "prettier": "^2.0.5",
    "sucrase": "^3.13.0",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "build": "sucrase-node build.ts",
    "lint": "prettier --list-different '**/*.{js,json,ts}'",
    "type-check": "tsc --noEmit -p ."
  }
}
